/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package q3continuing.app;

import q3continuing.list.LinkedList;

import static q3continuing.utilities.StringUtils.join;
import static q3continuing.utilities.StringUtils.split;
import static q3continuing.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
